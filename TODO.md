# TODO

- [ ] Update `app/profile.tsx` to use cascading Department → Course dropdown (replace manual typing for academicProgram).
- [x] Update `app/profile.tsx` to use dropdowns for Year level and Gender identity (replace manual typing).
- [x] Ensure Firestore save/load includes `department` and uses consistent field names.
- [ ] Run TypeScript check/build and do quick manual verification of the UI.
